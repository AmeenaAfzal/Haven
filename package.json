{
  "name": "haven-server",
  "version": "1.0.0",
  "description": "Local server that keeps the OpenAI key server-side and proxies calls for the Haven demo.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
